[project]
name = "sem-data-prep"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.0",
    "pyarrow>=14.0",
    "semopy>=2.3",
    "click>=8.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.0"]
